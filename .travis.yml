jobs:
  include:
  - stage: GitHub Release
    script: bash scripts/build.sh
    deploy:
      provider: releases
      api_key:
        secure: $GITHUB_OAUTH_TOKEN
      file:
      - release.zip
      - release.tgz
      skip_cleanup: true
      on:
        tags: true
